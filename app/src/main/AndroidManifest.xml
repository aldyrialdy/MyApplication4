<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pertemuan.lat.mobpro.myapplication">

    <application
        android:allowBackup="true"
        android:debuggable="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OptionMenu_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".RelativeLayoutExample" />
        <activity android:name=".LinearLayoutExample" />
        <activity android:name=".MainAppsActivity" />
        <activity android:name=".TableLayoutExample" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ServiceActivity"></activity>
    </application>

</manifest>
